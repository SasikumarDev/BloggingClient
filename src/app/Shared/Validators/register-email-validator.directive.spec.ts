import { RegisterEmailValidatorDirective } from './register-email-validator.directive';

describe('RegisterEmailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterEmailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
